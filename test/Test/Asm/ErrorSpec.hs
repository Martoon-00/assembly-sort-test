-- | Properties on failing executions of assembly program.

module Test.Asm.ErrorSpec
    ( spec
    ) where

import           Test.Hspec (Spec)
import           Universum


spec :: Spec
spec = do
    return ()

