-- | Benchmarks.

import           Criterion.Main (bgroup, defaultMain)
import           Universum

main :: IO ()
main = defaultMain
    [ bgroup "index build"
        [
        ]

    , bgroup "index build + queries"
        [
        ]

    ]

